import * as React from "react";
import StyledChapterGrid from "./styled";

const ChapterGrid = ({ children }) => {
	return <StyledChapterGrid>{children}</StyledChapterGrid>;
};

export default ChapterGrid;
